#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR=$(cd "$(dirname "$0")" && pwd)
PYTHON_BIN="${PYTHON:-python3}"
PYTHONPATH="${SCRIPT_DIR}/../src${PYTHONPATH:+:${PYTHONPATH}}" "${PYTHON_BIN}" -m license_manager_simulators.lmstat.cli "$@"
